# 연수원 연동 '전체 삭제' 기능 분석

## 삭제되는 데이터

### ✅ 완전히 삭제되는 항목

1. **MatchingResult** (매칭 결과)
   - 모든 멘토-멘티 매칭 결과 삭제
   - `matching_results` 테이블 전체 삭제

2. **MatchingReport** (매칭 리포트)
   - 모든 매칭 리포트 삭제
   - `matching_reports` 테이블 전체 삭제

3. **TrainingCenterRecord** (연수원 레코드)
   - 모든 멘티/멘토 연수원 데이터 삭제
   - `training_center_records` 테이블 전체 삭제

4. **TrainingCohort** (연수원 기수)
   - 모든 기수 메타데이터 삭제
   - `training_cohorts` 테이블 전체 삭제

## ❌ 삭제되지 않는 데이터 (남아있는 항목)

### 1. User 계정 (사용자 계정)
- **삭제 안 됨**: 연수원 연동으로 생성된 모든 User 계정이 그대로 남아있음
- **영향**: 
  - 멘티/멘토 계정이 모두 남아있어 로그인 가능
  - 하지만 연수원 데이터와의 연결이 끊어짐

### 2. ExamScore (시험 점수)
- **삭제 안 됨**: 모든 시험 점수가 그대로 남아있음
- **영향**: 
  - 사용자들의 시험 성적이 유지됨
  - 하지만 연수원 레코드와의 연결이 끊어짐

### 3. MentorMenteeRelation (멘토-멘티 관계)
- **삭제 안 됨**: 매칭으로 생성된 관계가 그대로 남아있을 수 있음
- **참고**: MatchingResult는 삭제되지만, MentorMenteeRelation은 별도 테이블

### 4. ChatHistory (채팅 기록)
- **삭제 안 됨**: 모든 챗봇 대화 기록이 남아있음

### 5. Feedback (피드백)
- **삭제 안 됨**: 모든 피드백이 남아있음

### 6. SimulationRecording (시뮬레이션 녹화)
- **삭제 안 됨**: 모든 시뮬레이션 녹화 기록이 남아있음

### 7. QuizGenerationLog (퀴즈 기록)
- **삭제 안 됨**: 모든 퀴즈 기록이 남아있음

### 8. 기타 사용자 관련 데이터
- 프로필 정보
- 학습 이력
- 기타 모든 사용자 활동 기록

## 삭제 순서

```python
def delete_all_records(self) -> int:
    """전체 연수원 데이터 삭제 (매칭 결과 포함)"""
    # 1. 매칭 결과 및 리포트 삭제
    self.session.exec(delete(MatchingResult))
    self.session.exec(delete(MatchingReport))
    
    # 2. 연수원 레코드 삭제
    self.session.exec(delete(TrainingCenterRecord))
    
    # 3. 연수원 기수 삭제
    self.session.exec(delete(TrainingCohort))
    
    self.session.commit()
```

## 결론

### ❌ 완전히 깔끔하게 삭제되지 않습니다

**삭제되는 것:**
- 연수원 원본 데이터 (TrainingCenterRecord, TrainingCohort)
- 매칭 결과 및 리포트 (MatchingResult, MatchingReport)

**남아있는 것:**
- User 계정 (연수원 연동으로 생성된 계정 포함)
- 시험 점수 (ExamScore)
- 멘토-멘티 관계 (MentorMenteeRelation)
- 모든 사용자 활동 기록 (채팅, 피드백, 시뮬레이션, 퀴즈 등)

## 문제점

1. **고아 데이터 발생**
   - User 계정은 남아있지만 연수원 데이터와의 연결이 끊어짐
   - `employee_number`로 연결되어 있지만 TrainingCenterRecord가 없어서 매칭 불가

2. **데이터 불일치**
   - 연수원 데이터는 삭제되었지만 User 계정과 관련 데이터는 남아있음
   - 시스템 상태가 일관성 없게 됨

3. **재매칭 불가**
   - 연수원 데이터가 없으면 매칭 실행 불가
   - User 계정만으로는 매칭에 필요한 정보 부족

## 권장 사항

### 옵션 1: User 계정도 함께 삭제 (완전 삭제)
```python
def delete_all_records(self) -> int:
    # 1. 매칭 결과 및 리포트 삭제
    self.session.exec(delete(MatchingResult))
    self.session.exec(delete(MatchingReport))
    
    # 2. 연수원 레코드 삭제
    self.session.exec(delete(TrainingCenterRecord))
    self.session.exec(delete(TrainingCohort))
    
    # 3. 연수원으로 생성된 User 계정 삭제 (employee_number 기준)
    training_employee_numbers = self.session.exec(
        select(TrainingCenterRecord.employee_number)
    ).all()
    
    # 이미 TrainingCenterRecord가 삭제되었으므로 다른 방법 필요
    # 또는 연수원 계정을 별도로 표시하여 삭제
    
    self.session.commit()
```

### 옵션 2: 확인 메시지 강화
현재 확인 메시지에 "User 계정은 삭제되지 않습니다" 명시

### 옵션 3: 선택적 삭제 옵션 제공
- "연수원 데이터만 삭제" (현재 방식)
- "연수원 데이터 + User 계정 삭제" (완전 삭제)

## 현재 확인 메시지

```typescript
if (!confirm('전체 연수원 데이터를 삭제하시겠습니까?\n(매칭 결과도 함께 삭제됩니다)')) {
  return
}
```

**개선 제안:**
```typescript
if (!confirm(
  '전체 연수원 데이터를 삭제하시겠습니까?\n\n' +
  '삭제되는 항목:\n' +
  '- 연수원 레코드 (멘티/멘토 데이터)\n' +
  '- 매칭 결과 및 리포트\n' +
  '- 연수원 기수 정보\n\n' +
  '남아있는 항목:\n' +
  '- User 계정 (로그인 가능)\n' +
  '- 시험 점수\n' +
  '- 채팅 기록\n' +
  '- 피드백\n' +
  '- 기타 사용자 활동 기록\n\n' +
  '주의: 연수원 데이터 삭제 후 재매칭이 불가능합니다.'
)) {
  return
}
```


