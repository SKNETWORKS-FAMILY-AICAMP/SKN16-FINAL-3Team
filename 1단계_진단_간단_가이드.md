# 1단계 진단 - 간단 가이드

## 🚀 가장 쉬운 방법: Python 스크립트 실행

### 준비사항
1. 백엔드 서버가 실행 중이어야 합니다
   ```bash
   # Docker 사용 시
   docker-compose ps
   
   # 또는 브라우저에서 확인
   http://localhost:8000 접속
   ```

2. Python이 설치되어 있어야 합니다 (requests 라이브러리 필요)
   ```bash
   pip install requests
   ```

### 실행 방법

프로젝트 루트 디렉토리에서 실행:

```bash
python diagnose_matching.py
```

### 출력 예시

```
============================================================
🔍 멘토-멘티 매칭 진단 시작
============================================================

1️⃣ 관리자 계정으로 로그인 중...
✅ 로그인 성공!

============================================================
2️⃣ 멘토 매칭 상태 진단
============================================================

📋 권도형 멘토 진단 중...
   👤 멘토 ID: 1001
   📧 이메일: ...
   📊 총 매칭 관계: 2개
   ✅ 활성 매칭: 0개  ← 문제 발견!
   ❌ 문제 발견: 활성 매칭이 없습니다!
   💡 해결 방법: 비활성 관계를 활성화하거나 재매칭이 필요합니다.

============================================================
3️⃣ 멘티 매칭 상태 진단
============================================================

📋 홍예미 멘티 진단 중...
   👤 멘티 ID: 1016
   📧 이메일: ...
   📊 총 매칭 관계: 1개
   ✅ 활성 매칭: 0개  ← 문제 발견!
   ❌ 문제 발견: 담당 멘토가 없습니다!
```

---

## 🌐 브라우저에서 확인 (Swagger UI)

### 1단계: Swagger UI 접속
```
http://localhost:8000/docs
```

### 2단계: 로그인하여 토큰 받기
1. `/auth/login` 찾기
2. "Try it out" 클릭
3. Request body 입력:
   ```json
   {
     "username": "admin@bank.com",
     "password": "admin123"
   }
   ```
4. "Execute" 클릭
5. Response에서 `access_token` 복사

### 3단계: Authorization 설정
1. 오른쪽 상단 "Authorize" 버튼 클릭
2. Value에 `Bearer {토큰}` 입력 (예: `Bearer eyJhbGc...`)
3. "Authorize" 클릭

### 4단계: 진단 API 호출

#### 권도형 멘토 진단
1. `/dashboard/admin/diagnose-matching/{mentor_name}` 찾기
2. "Try it out" 클릭
3. `mentor_name`에 `권도형` 입력
4. "Execute" 클릭
5. Response 확인

#### 홍예미/황나미 멘티 진단
1. `/dashboard/admin/diagnose-mentee-matching/{mentee_name}` 찾기
2. "Try it out" 클릭
3. `mentee_name`에 `홍예미` 또는 `황나미` 입력
4. "Execute" 클릭
5. Response 확인

---

## 📝 결과 해석

### 정상적인 경우
- `active_count` > 0
- `active_relations` 배열에 데이터가 있음

### 문제가 있는 경우
- `active_count` = 0 → 활성 매칭이 없음
- `all_relations`에 데이터가 있지만 모두 `is_active: false` → 비활성화된 매칭
- `all_relations`가 빈 배열 → 매칭 관계 자체가 없음

---

## ❓ 문제 해결

진단 결과를 확인한 후:
1. `backend/매칭_문제_해결_가이드.md` 파일 참고
2. 관리자 대시보드에서 재매칭 수행
3. 필요시 DB에서 직접 수정

더 자세한 방법은 `backend/1단계_문제_진단_방법.md` 파일을 참고하세요!



