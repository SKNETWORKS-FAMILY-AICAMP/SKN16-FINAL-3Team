# 관리자 대시보드 기능 문제 진단 및 해결방안

## 문제 현상
- 세 가지 기능(연수원 연동, 성적 생성, 매칭 실행)이 작동하지 않음
- 백엔드 컨테이너의 disk write가 0B로 유지됨
- 로그에서 500 Internal Server Error 발생

## 진단 결과

### 1. 데이터베이스 상태
- ✅ 데이터베이스 연결 정상 (`postgresql://mentoruser:mentorpass@postgres:5432/mentordb`)
- ✅ 디스크 공간 충분 (925G 사용 가능)
- ✅ 데이터베이스 크기: 63MB (정상)
- ✅ 기존 데이터 존재:
  - `training_center_records`: 180개
  - `exam_scores`: 121개
  - `mentor_mentee_relations`: 1개

### 2. 로그 분석
- ❌ `/admin/learning-history/seed-prequiz` API가 500 에러 반환
- ⚠️ 모든 SELECT 쿼리 후 ROLLBACK 발생 (정상 - SELECT는 자동 커밋 불필요)
- ⚠️ 실제 에러 메시지가 로그에 표시되지 않음

### 3. 코드 검증
- ✅ `create_initial_exam_score` 함수는 정상 작동 (직접 테스트 완료)
- ✅ 세션 커밋 로직은 코드에 존재
- ⚠️ FastAPI 에러 핸들링에서 실제 에러 메시지가 숨겨질 수 있음

## 가능한 원인

### 1. 트랜잭션 롤백 문제 (가장 가능성 높음)
**증상**: 모든 작업이 ROLLBACK으로 끝남
**원인**:
- FastAPI의 `get_session()` dependency가 세션을 자동으로 닫을 때 롤백 발생
- 명시적 `session.commit()` 호출이 있어도 예외 발생 시 롤백됨

**확인 방법**:
```python
# backend/app/database.py의 get_session() 함수 확인
def get_session():
    with Session(engine) as session:
        yield session
        # 여기서 자동으로 세션이 닫히면서 롤백될 수 있음
```

### 2. 예외 처리 문제
**증상**: 500 에러 발생하지만 실제 에러 메시지가 로그에 없음
**원인**:
- 예외가 발생했지만 로깅되지 않음
- FastAPI의 기본 에러 핸들러가 에러를 숨김

### 3. 디스크 쓰기 권한 문제
**증상**: disk write가 0B
**원인**:
- PostgreSQL 볼륨 마운트 문제
- 컨테이너 내부 디스크 권한 문제

## 해결 방안

### 즉시 조치 사항

#### 1. 상세 에러 로그 확인
```bash
# 백엔드 컨테이너 로그에서 실제 에러 확인
docker logs mentor-backend --tail 1000 | Select-String -Pattern "Traceback|Exception|Error" -Context 10,20

# 또는 실시간 로그 모니터링
docker logs -f mentor-backend
```

#### 2. API 직접 테스트
```bash
# 성적 생성 API 테스트
curl -X POST http://localhost:8000/admin/learning-history/seed-prequiz \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -v

# 연수원 연동 API 테스트
curl -X POST http://localhost:8000/training-center/sync \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"selected_cohort_dates": ["2025-01-01"], "create_accounts": true, "create_mentees": true, "create_mentors": true}' \
  -v
```

#### 3. 세션 관리 개선
`backend/app/database.py`의 `get_session()` 함수를 다음과 같이 수정:

```python
def get_session():
    """
    데이터베이스 세션 의존성
    FastAPI 엔드포인트에서 사용됩니다.
    """
    session = Session(engine)
    try:
        yield session
        session.commit()  # 명시적 커밋
    except Exception:
        session.rollback()  # 에러 시 롤백
        raise
    finally:
        session.close()  # 세션 닫기
```

**주의**: 이 방법은 모든 요청을 자동 커밋하므로, 명시적 롤백이 필요한 경우 문제가 될 수 있습니다.

#### 4. 에러 로깅 강화
`backend/app/routers/admin.py`의 에러 처리 개선:

```python
@router.post("/learning-history/seed-prequiz")
async def seed_exam_scores(
    current_user: User = Depends(require_admin),
    session: Session = Depends(get_session),
):
    try:
        # ... 기존 코드 ...
    except HTTPException:
        session.rollback()
        raise
    except Exception as e:
        import traceback
        error_trace = traceback.format_exc()
        print(f"❌ 시험 점수 생성 실패: {str(e)}")
        print(f"Traceback: {error_trace}")
        session.rollback()
        raise HTTPException(
            status_code=500, 
            detail=f"시험 점수 생성 실패: {str(e)}\n{error_trace}"
        )
```

### 근본 원인 해결

#### 1. PostgreSQL 볼륨 확인
```bash
# 볼륨 마운트 확인
docker inspect mentor-postgres | Select-String -Pattern "Mounts" -Context 0,20

# 볼륨 디렉토리 권한 확인
ls -la postgres_data/
```

#### 2. 디스크 I/O 모니터링
```bash
# PostgreSQL 컨테이너의 실제 디스크 사용량 확인
docker exec mentor-postgres du -sh /var/lib/postgresql/data

# WAL (Write-Ahead Log) 파일 확인
docker exec mentor-postgres ls -lh /var/lib/postgresql/data/pg_wal/
```

#### 3. 트랜잭션 로그 확인
```bash
# PostgreSQL 트랜잭션 로그 확인
docker exec mentor-postgres psql -U mentoruser -d mentordb -c "SELECT * FROM pg_stat_database WHERE datname = 'mentordb';"
```

## 권장 조치 순서

1. **즉시**: 상세 에러 로그 확인 및 API 직접 테스트
2. **단기**: 에러 로깅 강화 및 세션 관리 개선
3. **중기**: PostgreSQL 볼륨 및 디스크 I/O 모니터링
4. **장기**: 트랜잭션 관리 및 에러 핸들링 체계화

## 추가 확인 사항

### 메모리 정리 후 영향
사용자가 언급한 "어제 메모리까지 다 정리했는데"라는 부분에서:
- PostgreSQL의 shared_buffers 설정이 변경되었을 수 있음
- WAL 파일이 정리되면서 트랜잭션 로그가 손상되었을 수 있음
- 데이터베이스 연결 풀이 초기화되었을 수 있음

**확인 방법**:
```bash
# PostgreSQL 설정 확인
docker exec mentor-postgres psql -U mentoruser -d mentordb -c "SHOW shared_buffers;"
docker exec mentor-postgres psql -U mentoruser -d mentordb -c "SHOW max_connections;"

# 연결 상태 확인
docker exec mentor-postgres psql -U mentoruser -d mentordb -c "SELECT count(*) FROM pg_stat_activity;"
```

## 다음 단계

1. 실제 에러 메시지 확인 (가장 중요)
2. 세션 관리 코드 수정
3. 에러 로깅 강화
4. PostgreSQL 상태 점검





