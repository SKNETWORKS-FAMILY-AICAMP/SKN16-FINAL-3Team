# RAG 정확도 개선 분석 보고서

## 📊 현재 정확도 현황

### 시나리오별 정확도
- **수신**: 80.0% (4/5) ✅
- **여신**: 66.7% (4/6) ⚠️
- **카드**: 75.0% (6/8) ⚠️

### 카테고리별 정확도
- **수신**: 금리 100%, 우대금리 0%, 가입금액 100%, 기간 100%, 세금 100%
- **여신**: 금리 0%, 한도 100%, LTV 100%, 기간 100%, 상환방식 100%, 필요서류 0%
- **카드**: 한도 100%, 금리 100%, 수수료 0%, 혜택 100%

---

## 🔍 주요 문제점 분석

### 1. 잘못된 청크 선택 문제 (P0 - 최우선)

#### 문제 사례

**사례 1: "이자소득세 15.4%" claim**
- **선택된 청크**: "제7조 중도해지" (유사도 0.864)
- **실제 정확한 청크**: "세금" (유사도 0.902) ✅
- **문제**: 첫 번째 청크가 아닌 다른 청크가 선택됨
- **원인**: 숫자 매칭 점수(30점)가 유사도 점수(50점)보다 낮아서, 숫자가 일치하는 청크를 선택하지 못함

**사례 2: "1개월 미만은 이자가 없고" claim**
- **선택된 청크**: "가입 기간" (유사도 0.838)
- **실제 정확한 청크**: "제7조 중도해지" (유사도 0.864) ✅
- **문제**: 카테고리 불일치로 인한 잘못된 청크 선택
- **원인**: claim의 카테고리가 "조건"으로 추출되어 "중도해지" 청크를 찾지 못함

**사례 3: "체크카드 사용분은 소득공제율이 30%" claim**
- **선택된 청크**: "제6조 연회비 및 수수료" (유사도 0.855)
- **실제 정확한 청크**: "소득공제 기본 정보" (유사도 0.908) ✅
- **문제**: 카테고리가 "수수료"로 설정되어 "소득공제율" 청크를 찾지 못함
- **원인**: 카테고리 필터링으로 인한 검색 범위 제한

#### 개선 방안

1. **카테고리 매핑 개선**
   - "소득공제율" → "수수료"가 아닌 "세금" 또는 "혜택" 카테고리로 매핑
   - "중도해지율" → "조건"이 아닌 "금리" 또는 "수수료" 카테고리로 매핑

2. **청크 선택 로직 개선**
   - 카테고리 불일치 시에도 관련 청크를 검색하도록 fallback 로직 추가
   - claim의 키워드와 청크의 subsection_title 매칭 점수 추가

3. **다중 청크 통합 검증**
   - 하나의 claim이 여러 청크에 걸쳐 있는 경우, 모든 관련 청크를 통합하여 검증

---

### 2. LLM의 과도한 엄격성 문제 (P1)

#### 문제 사례

**사례 1: "최대 연 2.65%" claim**
- **Ground Truth**: 12개월 계약기간의 최고금리 2.65% ✅
- **LLM 판단**: "12개월 계약에만 해당하므로 부정확" ❌
- **실제 발화**: "현재 12개월 기준 기본 금리는 연 2.15%이고, 우대조건을 모두 충족하시면 최대 연 2.65%까지 가능해요"
- **문제**: 발화에서 "12개월 기준"이라고 명시했지만, LLM이 이를 인식하지 못함
- **원인**: LLM 검증 시 full_utterance 컨텍스트가 충분히 전달되지 않음

**사례 2: "중도해지 금리가 적용돼서 약정금리보다 낮은 이자만 받으실 수 있습니다" claim**
- **LLM Reasoning**: "주장은 정확합니다" ✅
- **is_accurate**: false ❌
- **문제**: LLM reasoning과 최종 판단이 불일치
- **원인**: 숫자 불일치 체크 로직이 LLM 결과를 무시함

#### 개선 방안

1. **LLM 프롬프트 개선**
   - full_utterance 컨텍스트를 LLM에 전달하여 발화 맥락 고려
   - "12개월 기준" 같은 조건부 표현을 인식하도록 프롬프트 강화

2. **LLM 결과 신뢰도 향상**
   - LLM reasoning이 명확히 정확하다고 판단한 경우, 숫자 불일치 체크를 완화
   - LLM confidence 점수를 활용한 가중치 조정

---

### 3. 카테고리 불일치로 인한 검색 실패 (P1)

#### 문제 사례

**사례: "체크카드 사용분은 소득공제율이 30%"**
- **추출된 카테고리**: "수수료"
- **실제 카테고리**: "소득공제" 또는 "세금"
- **검색 결과**: 카테고리 필터링으로 인해 "소득공제" 청크를 찾지 못함
- **해결책**: 키워드 검색으로 fallback했지만, 벡터 검색이 더 정확함

#### 개선 방안

1. **카테고리 추출 개선**
   - "소득공제율" → "수수료"가 아닌 "세금" 또는 "혜택"으로 매핑
   - 카테고리 추출 시 키워드 기반 매핑 테이블 활용

2. **카테고리 필터링 완화**
   - 카테고리 필터링 실패 시, 카테고리 필터 없이 재검색
   - 관련 카테고리 목록을 확장하여 검색 범위 넓히기

---

### 4. 다중 청크 정보 통합 필요 (P2)

#### 문제 사례

**사례: "이자소득세 15.4%" claim**
- **matched_chunks**: 5개 청크 발견
  - "세금" (유사도 0.902) ✅
  - "계산 예시" (유사도 0.868)
  - "세무 관련" (유사도 0.863)
  - "제7조 중도해지" (유사도 0.864) ❌
  - "제8조 중도해지 금리표" (유사도 0.849)
- **선택된 청크**: "제7조 중도해지" (잘못된 선택)
- **문제**: 가장 유사도가 높은 "세금" 청크가 선택되지 않음

#### 개선 방안

1. **청크 선택 로직 개선**
   - subsection_title과 claim의 키워드 매칭 점수 추가
   - "이자소득세" claim → "세금" subsection_title 매칭 점수 높임

2. **다중 청크 통합 검증**
   - 하나의 claim이 여러 청크에 걸쳐 있는 경우, 모든 관련 청크를 통합하여 검증
   - 각 청크의 정확성을 개별 평가하고, 하나라도 정확하면 claim을 정확으로 판단

---

## 🎯 개선 우선순위

### P0 (최우선): 잘못된 청크 선택 문제 해결
1. **카테고리 매핑 개선**
   - "소득공제율" → "세금" 또는 "혜택" 카테고리로 매핑
   - "중도해지율" → "금리" 또는 "수수료" 카테고리로 매핑

2. **청크 선택 로직 개선**
   - subsection_title과 claim의 키워드 매칭 점수 추가 (20점)
   - 카테고리 불일치 시에도 관련 청크를 검색하도록 fallback 로직 추가

### P1 (높음): LLM 검증 개선
1. **LLM 프롬프트 개선**
   - full_utterance 컨텍스트를 LLM에 전달
   - "12개월 기준" 같은 조건부 표현을 인식하도록 프롬프트 강화

2. **LLM 결과 신뢰도 향상**
   - LLM reasoning이 명확히 정확하다고 판단한 경우, 숫자 불일치 체크를 완화

### P2 (중간): 카테고리 필터링 완화
1. **카테고리 필터링 실패 시 재검색**
   - 카테고리 필터링 실패 시, 카테고리 필터 없이 재검색
   - 관련 카테고리 목록을 확장하여 검색 범위 넓히기

### P3 (낮음): 다중 청크 통합 검증
1. **다중 청크 통합 검증**
   - 하나의 claim이 여러 청크에 걸쳐 있는 경우, 모든 관련 청크를 통합하여 검증

---

## 📈 예상 개선 효과

### P0 개선 시 예상 효과
- **수신**: 80.0% → 100% (잘못된 청크 선택 문제 해결)
- **여신**: 66.7% → 83.3% (카테고리 불일치 문제 해결)
- **카드**: 75.0% → 87.5% (소득공제율 청크 선택 문제 해결)

### P1 개선 시 예상 효과
- **수신**: 100% → 100% (유지)
- **여신**: 83.3% → 100% (LLM 엄격성 문제 해결)
- **카드**: 87.5% → 100% (LLM 엄격성 문제 해결)

### 전체 예상 효과
- **수신**: 80.0% → 100% (+20.0%p)
- **여신**: 66.7% → 100% (+33.3%p)
- **카드**: 75.0% → 100% (+25.0%p)

---

## 🔧 구현 방법 (일반 모드 호환성 보장)

### ⚠️ 중요: 일반 모드 호환성 확인

모든 개선 방안은 **일반 모드와 테스트 모드 모두에서 안정적으로 작동**하도록 설계되었습니다:

1. **`expected_keywords`는 Optional**: 일반 모드에서 None이어도 키워드 점수는 0점으로 처리되어 문제없음
2. **카테고리 매핑**: claim의 키워드를 기반으로 하므로 일반 모드의 다양한 발화에도 적용 가능
3. **subsection_title 매칭**: claim에서 키워드를 자동 추출하므로 일반 모드에서도 안정적
4. **full_utterance 컨텍스트**: 이미 일반 모드에서도 전달되고 있음

### P0-1: 카테고리 매핑 개선 (일반 모드 호환)

```python
# backend/app/services/product_knowledge_service.py

# 카테고리 매핑 테이블 (일반 모드의 다양한 발화에도 적용 가능)
CATEGORY_MAPPING = {
    # 소득공제 관련 (다양한 표현 지원)
    "소득공제": ["세금", "혜택", "수수료"],
    "소득공제율": ["세금", "혜택", "수수료"],
    "소득 공제": ["세금", "혜택", "수수료"],
    "공제율": ["세금", "혜택", "수수료"],
    "세제혜택": ["세금", "혜택"],
    
    # 중도해지 관련
    "중도해지": ["금리", "수수료", "조건"],
    "중도해지율": ["금리", "수수료", "조건"],
    "중도 해지": ["금리", "수수료", "조건"],
    "중도해지 금리": ["금리", "수수료"],
    
    # 이자소득세 관련
    "이자소득세": ["세금", "수수료"],
    "이자 소득세": ["세금", "수수료"],
    "원천징수": ["세금", "수수료"],
    "세후이자": ["세금", "수수료"],
    
    # 연회비 관련
    "연회비": ["수수료"],
    "연 회비": ["수수료"],
    "회비": ["수수료"],
    
    # 우대금리 관련
    "우대금리": ["금리", "혜택"],
    "우대 금리": ["금리", "혜택"],
    "가산금리": ["금리", "혜택"],
}

def _get_category_for_claim(self, claim: str, extracted_category: str) -> List[str]:
    """
    claim의 키워드를 기반으로 카테고리 목록 반환
    일반 모드의 다양한 발화에도 적용 가능 (키워드 기반 매칭)
    """
    categories = [extracted_category] if extracted_category else []
    
    claim_lower = claim.lower()
    
    # 키워드 기반 카테고리 매핑 (부분 매칭 지원)
    for keyword, mapped_categories in CATEGORY_MAPPING.items():
        if keyword.lower() in claim_lower:
            categories.extend(mapped_categories)
            break  # 첫 번째 매칭만 사용 (우선순위)
    
    # 중복 제거 및 기본 카테고리 보장
    if not categories:
        categories = ["기타"]  # 기본 카테고리
    
    return list(set(categories))  # 중복 제거
```

### P0-2: 청크 선택 로직 개선 (일반 모드 호환)

```python
# subsection_title과 claim의 키워드 매칭 점수 추가
# 일반 모드에서도 안정적으로 작동 (claim에서 키워드 자동 추출)

def _calculate_subsection_match_score(self, claim: str, chunk: Dict) -> float:
    """
    subsection_title과 claim의 키워드 매칭 점수 계산
    일반 모드의 다양한 발화에도 적용 가능 (키워드 자동 추출)
    """
    subsection_title = chunk.get("subsection_title", "").lower()
    claim_lower = claim.lower()
    
    # 키워드 추출 (일반 모드에서도 작동)
    claim_keywords = self._extract_search_keywords(claim)  # 기존 메서드 활용
    
    # 매칭 점수 계산
    match_score = 0.0
    
    # 1. 직접 키워드 매칭
    for keyword in claim_keywords:
        keyword_lower = keyword.lower()
        if keyword_lower in subsection_title:
            match_score += 2.0  # 직접 매칭은 높은 점수
        elif any(kw in subsection_title for kw in keyword_lower.split() if len(kw) > 1):
            match_score += 1.0  # 부분 매칭은 낮은 점수
    
    # 2. 카테고리 키워드 매칭 (DEFAULT_SUBSECTION_KEYWORDS 활용)
    for category, keywords in DEFAULT_SUBSECTION_KEYWORDS.items():
        if category.lower() in subsection_title:
            # claim에 해당 카테고리 키워드가 있는지 확인
            if any(kw in claim_lower for kw in keywords):
                match_score += 3.0  # 카테고리 매칭은 매우 높은 점수
                break
    
    # 정규화 (최대 20점)
    return min(match_score * 2.0, 20.0)
```

### P1-1: LLM 프롬프트 개선 (일반 모드 호환)

```python
# full_utterance 컨텍스트를 LLM에 전달
# 일반 모드에서도 안정적으로 작동 (conversation_history에서 full_utterance 추출)

def _verify_with_llm(self, claim: str, ground_truth: str, category: str, 
                     product_code: str, full_utterance: Optional[str] = None,
                     conversation_context: Optional[str] = None):
    """
    LLM 기반 사실 검증
    일반 모드와 테스트 모드 모두에서 안정적으로 작동
    """
    # conversation_context가 있으면 우선 사용 (더 많은 맥락 정보)
    context_text = conversation_context if conversation_context else full_utterance
    
    prompt = f"""제품 정보 사실 검증을 수행하세요.
    
    **전체 발화 맥락 (문맥 정보):**
    {context_text if context_text else "없음"}
    
    **사용자 주장 (Claim):**
    {claim}
    
    **제품 지식 베이스 정보 (Ground Truth):**
    {ground_truth}
    
    **중요 지침:**
    1. 전체 발화 맥락을 고려하여 claim의 정확성을 판단하세요.
    2. 조건부 표현을 인식하세요:
       - "12개월 기준" → 12개월 계약기간에 대한 정보로 해석
       - "우대조건 충족 시" → 우대금리 적용 조건을 고려
       - "최대" → 최대값을 의미 (특정 조건에서만 달성 가능)
    3. Ground Truth에 명시된 정보만을 기준으로 판단하세요.
    4. 숫자 정보는 정확히 일치해야 합니다 (0.01 이내 오차 허용).
    5. 의미적으로 동일한 표현은 정확한 것으로 판단하세요.
    
    **판단 기준:**
    - 정확 (Accurate): Claim이 Ground Truth와 일치하거나 의미적으로 동일함
    - 부정확 (Inaccurate): Claim이 Ground Truth와 불일치하거나 Ground Truth에 없는 정보임
    """
```

---

## 📝 다음 단계

1. **P0 개선 구현** (카테고리 매핑 + 청크 선택 로직)
   - ✅ 일반 모드 호환성 확인 완료
   - ✅ `expected_keywords` Optional 처리 확인 완료
   - ✅ 키워드 자동 추출 방식으로 일반 모드에서도 안정적 작동 보장
2. **테스트 실행 및 검증**
   - 테스트 모드: 고정 시나리오로 검증
   - 일반 모드: 다양한 발화로 안정성 검증 (선택적)
3. **P1 개선 구현** (LLM 프롬프트 개선)
   - ✅ `conversation_context` 활용으로 일반 모드에서도 맥락 정보 전달
4. **최종 테스트 및 정확도 확인**

---

## ✅ 일반 모드 호환성 보장 체크리스트

- [x] `expected_keywords` Optional 처리 (None일 때도 정상 작동)
- [x] 카테고리 매핑: 키워드 기반 부분 매칭으로 다양한 발화 지원
- [x] subsection_title 매칭: claim에서 키워드 자동 추출
- [x] full_utterance 컨텍스트: conversation_history에서 추출 가능
- [x] 하드코딩된 값 최소화: 동적 키워드 추출 방식 사용
- [x] 테스트 모드 전용 로직 없음: 모든 로직이 일반 모드에서도 작동

