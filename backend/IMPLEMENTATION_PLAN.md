# 멘토 배정 및 초기화 로직 개선 구현 계획

## 완료된 작업

### 1. 초기화 로직 수정 (`initialize_demo_data`)
- ✅ 1-3기 데이터는 기존 데이터 확인 후 있으면 스킵
- ✅ 4기 데이터만 삭제 및 재생성
- ✅ `_run_matching_all_cohorts()` 호출로 변경

### 2. 4기 데이터만 삭제하는 메서드 추가 (`_delete_cohort_4_data`)
- ✅ 4기 관련 데이터만 선택적으로 삭제
- ✅ 1-3기 데이터는 보존

## 남은 작업

### 3. 매칭 로직 개선 (`_run_matching_all_cohorts()` 메서드 추가)
각 기수별로 독립적인 15명의 멘토만 할당하도록 구현해야 합니다.

**현재 문제:**
- `_run_matching_for_cohort_4()` 메서드가 모든 멘토를 가져와서 사용
- 멘토가 여러 기수를 담당할 수 있음

**해결 방법:**
- 각 기수(1, 2, 3, 4기)별로 해당 기수의 멘토만 조회
- 각 기수의 멘티를 해당 기수의 멘토에게만 할당
- 멘토당 2명의 멘티 할당

**구현 내용:**
```python
def _run_matching_all_cohorts(self) -> Dict[str, int]:
    """각 기수별로 독립적인 멘토 할당하여 매칭 실행"""
    # 1. 모든 기수 조회 (1, 2, 3, 4기)
    # 2. 각 기수별로:
    #    - 해당 기수의 멘티 조회
    #    - 해당 기수의 멘토만 조회 (cohort_id가 같은 멘토)
    #    - 멘토당 2명의 멘티 할당
    #    - MentorMenteeRelation 생성 (cohort_id 포함)
    #    - 멘토의 cohort_label 업데이트
```

## 다음 단계

1. `_run_matching_all_cohorts()` 메서드 구현
2. 테스트 및 검증
3. 기존 매칭 관계 정리 (필요시)

## 참고사항

- 각 기수의 seed 파일에 이미 15명의 멘토가 포함되어 있음
- 매칭 시 각 기수의 멘토만 사용하면 자연스럽게 독립적인 할당이 됨
- 초기화 후에도 1-3기 데이터는 유지됨

