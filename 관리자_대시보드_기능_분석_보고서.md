# 관리자 대시보드 기능 분석 보고서

## 개요
본 보고서는 관리자 대시보드의 세 가지 핵심 기능에 대한 상세 분석을 제공합니다:
1. **연수원 연동** (Training Center Sync)
2. **성적 생성** (Seed Pre-Quiz History)
3. **매칭 실행** (Matching Run)

---

## 1. 연수원 연동 기능

### 1.1 기능 개요
- **위치**: 관리자 대시보드 > 연수원 연동 탭
- **목적**: 연수원 데이터를 동기화하여 멘티/멘토 데이터와 사용자 계정을 생성
- **주요 기능**:
  - 기수별 멘티/멘토 데이터 생성
  - 사용자 계정 자동 생성
  - 연수원 데이터 조회 및 관리

### 1.2 백엔드 구조

#### API 엔드포인트
```58:99:backend/app/routers/training_center.py
@router.post("/sync", response_model=TrainingCenterSyncResponse)
async def sync_training_center_data(
    request: TrainingCenterSyncRequest,
    current_user: User = Depends(require_admin),
    session: Session = Depends(get_session),
):
    """연수원 DB 재구성 (신입 멘티, 멘토 풀 생성)
    
    Request body:
        selected_cohort_dates: List[str] - 생성할 기수 날짜 리스트 (YYYY-MM-DD 형식)
        create_accounts: bool - User 계정도 함께 생성할지 여부
        create_mentees: bool - 멘티 생성 여부
        create_mentors: bool - 멘토 생성 여부
    """
```

#### 서비스 로직
- **파일**: `backend/app/services/training_center_service.py`
- **주요 메서드**:
  - `rebuild_dataset()`: 데이터셋 재구성 (기존 데이터 삭제 후 재생성)
  - `_generate_records_for_cohort()`: 기수별 레코드 생성
  - `_create_user_accounts()`: 사용자 계정 생성
  - `list_records()`: 레코드 조회 (페이징, 필터링)

#### 데이터 생성 규칙
```212:346:backend/app/services/training_center_service.py
    def rebuild_dataset(
        self,
        selected_cohort_dates: Optional[List[date]] = None,
        create_accounts: bool = False,
        create_mentees: bool = True,
        create_mentors: bool = True,
    ) -> Dict[str, Any]:
        """신입(멘티)·기존 사원(멘토) 데이터를 초기화 후 재생성
        
        Args:
            selected_cohort_dates: 생성할 기수 날짜 리스트 (None이면 모든 기수 생성)
            create_accounts: User 계정도 함께 생성할지 여부
            create_mentees: 멘티 생성 여부
            create_mentors: 멘토 생성 여부
        """
        # 매칭 결과/리포트 → 연수원 레코드 순으로 삭제 (FK 보호)
        self.session.exec(delete(MatchingResult))
        self.session.exec(delete(MatchingReport))
        self.session.exec(delete(TrainingCenterRecord))
        self.session.exec(delete(TrainingCohort))
        self.session.commit()
```

**특징**:
- 한 기수당 멘티 120명 생성 (`MONTHLY_NEWCOMERS = 120`)
- 멘티:멘토 비율 2:1 (`MENTOR_POOL_SIZE = 60`)
- MBTI, 거주지, 전공 등은 가중치 기반 분포 생성
- 사번은 이름 순서 또는 입사년도별로 부여

### 1.3 프론트엔드 구조

#### 컴포넌트 위치
```5172:5466:frontend/src/pages/Dashboard.tsx
// 연수원 연동 탭: 멘티 시험 엑셀 업로드 및 처리 결과 표시
function TrainingSyncTab() {
  type TrainingFilters = { cohortDate: string; search: string }
  type TrainingCategory = 'mentee' | 'mentor'
  const SCORE_COLUMNS = TRAINING_LEARNING_SECTIONS
  const CATEGORY_TABS: { key: TrainingCategory; label: string; description: string }[] = [
    { key: 'mentee', label: '신입 멘티', description: '연수원 기수별 신입 30명/월' },
    { key: 'mentor', label: '기존 멘토', description: '창구사무 선배 직원 풀' }
  ]
```

#### 주요 기능
1. **기수 선택 및 동기화**
   - 체크박스로 기수 선택 (2025년 1기~4기)
   - 멘티/멘토 생성 옵션 선택
   - 동기화 실행 버튼

2. **데이터 조회**
   - 멘티/멘토 탭 전환
   - 기수별 필터링
   - 검색 기능 (이름, 사번)
   - 컬럼별 필터링

3. **데이터 관리**
   - 선택된 레코드 일괄 삭제
   - 전체 데이터 삭제
   - 엑셀 업로드 (레거시 기능)

### 1.4 발견된 이슈 및 개선점

#### ✅ 정상 동작
- 데이터 생성 로직이 체계적으로 구현됨
- 가중치 기반 분포 생성으로 현실적인 데이터 생성
- FK 제약 조건을 고려한 삭제 순서

#### ⚠️ 개선 필요 사항
1. **데이터 삭제 시 주의사항**
   - 전체 데이터 삭제 시 매칭 결과도 함께 삭제됨 (의도된 동작)
   - 사용자에게 명확한 경고 메시지 필요

2. **에러 처리**
   - 동기화 실패 시 상세한 에러 메시지 제공
   - 부분 실패 시 롤백 처리 필요

3. **성능 최적화**
   - 대량 데이터 생성 시 진행률 표시 필요
   - 비동기 처리 고려

---

## 2. 성적 생성 기능

### 2.1 기능 개요
- **위치**: 관리자 대시보드 > 학습 이력 탭 > 성적 생성 버튼
- **목적**: 멘티/멘토의 초기/중간/최종 시험 성적을 자동 생성
- **주요 기능**:
  - 멘티: 초기 평가(ExamType.BEGINNING)만 생성
  - 멘토: 초기/중간/최종 평가를 상승 곡선으로 생성

### 2.2 백엔드 구조

#### API 엔드포인트
```1298:1336:backend/app/routers/admin.py
@router.post("/learning-history/seed-prequiz")
async def seed_exam_scores(
    current_user: User = Depends(require_admin),
    session: Session = Depends(get_session),
):
    """
    학습 이력 탭의 '성적 생성' 기능
    - 멘티: 초기 평가(ExamType.BEGINNING)만 생성
    - 멘토: 초기/중간/최종 평가를 상승 곡선으로 생성
    """
    try:
        users = session.exec(
            select(User).where(
                User.role != UserRole.ADMIN,
                User.is_active == True,  # noqa: E712
            )
        ).all()

        mentee_created = 0
        mentor_created = 0

        for user in users:
            if user.role == UserRole.MENTEE:
                mentee_created += _ensure_beginning_exam_for_mentee(session, user)
            elif user.role == UserRole.MENTOR:
                mentor_created += _ensure_progressive_exams_for_mentor(session, user)

        session.commit()
        return {
            "message": f"멘티 {mentee_created}명, 멘토 {mentor_created}명의 시험 점수를 생성했습니다.",
            "mentees_created": mentee_created,
            "mentors_created": mentor_created,
        }
```

#### 성적 생성 로직

**멘티 성적 생성**:
```70:80:backend/app/routers/admin.py
def _ensure_beginning_exam_for_mentee(session: Session, user: User) -> int:
    exists = session.exec(
        select(ExamScore).where(
            ExamScore.mentee_id == user.id,
            ExamScore.exam_type == ExamType.BEGINNING,
        )
    ).first()
    if exists:
        return 0
    create_initial_exam_score(user.id, session, exam_type=ExamType.BEGINNING, commit=False)
    return 1
```

**멘토 성적 생성** (상승 곡선):
```83:114:backend/app/routers/admin.py
def _ensure_progressive_exams_for_mentor(session: Session, user: User) -> int:
    created = 0
    previous_total: Optional[float] = None
    for exam_type, (low, high) in MENTOR_PROGRESSIVE_RANGES:
        exists = session.exec(
            select(ExamScore).where(
                ExamScore.mentee_id == user.id,
                ExamScore.exam_type == exam_type,
            )
        ).first()
        if exists:
            previous_total = exists.total_score
            continue
        target = random.randint(low, high)
        if previous_total is not None:
            target = max(target, int(previous_total) + random.randint(1, 3))
        target = min(60, target)
        section_scores = _generate_section_scores_for_total(target)
        feedback = f"{EXAM_TYPE_LABELS.get(exam_type, '시험')} 결과가 기록되었습니다."
        create_initial_exam_score(
            user.id,
            session,
            exam_type=exam_type,
            section_scores_override=section_scores,
            total_score_override=sum(section_scores.values()),
            feedback=feedback,
            exam_name=EXAM_TYPE_LABELS.get(exam_type, "연수원 평가"),
            commit=False,
        )
        created += 1
        previous_total = sum(section_scores.values())
    return created
```

**점수 범위**:
```46:50:backend/app/routers/admin.py
MENTOR_PROGRESSIVE_RANGES = [
    (ExamType.BEGINNING, (38, 46)),
    (ExamType.MIDTERM, (46, 54)),
    (ExamType.FINAL, (54, 60)),
]
```

### 2.3 프론트엔드 구조

#### UI 컴포넌트
```4835:4847:frontend/src/pages/Dashboard.tsx
  const handleSeedPreQuiz = async () => {
    if (!confirm('모든 멘티/멘토 시험 성적(초기·중간·최종)을 자동 생성합니다.\n이미 존재하는 성적은 유지됩니다. 계속하시겠습니까?')) return
    try {
      setLoading(true)
      const res = await adminAPI.seedPreQuizHistory()
      alert(res.message || '성적 생성이 완료되었습니다.')
      loadHistory()
    } catch (error: any) {
      alert(error?.response?.data?.detail || error?.message || '생성 실패')
    } finally {
      setLoading(false)
    }
  }
```

### 2.4 발견된 이슈 및 개선점

#### ✅ 정상 동작
- 기존 성적이 있으면 건너뛰는 안전한 로직
- 멘토의 상승 곡선 생성으로 현실적인 데이터 생성
- 섹션별 점수 균등 분배 로직

#### ⚠️ 개선 필요 사항
1. **진행률 표시**
   - 대량 사용자 처리 시 진행률 표시 필요
   - 비동기 처리로 UI 블로킹 방지

2. **에러 처리**
   - 개별 사용자 생성 실패 시 상세 로그 필요
   - 부분 실패 시 롤백 또는 재시도 로직

3. **성적 생성 규칙 명확화**
   - 멘티는 초기만, 멘토는 3단계 모두 생성
   - 사용자에게 명확한 안내 필요

---

## 3. 매칭 실행 기능

### 3.1 기능 개요
- **위치**: 관리자 대시보드 > 매칭 시스템 탭
- **목적**: N차원 분류 기반으로 멘토-멘티 자동 매칭
- **주요 기능**:
  - 팀 > 약점-강점 > 커리어 > 거주지 > 취미 > 전공 순서로 매칭
  - 매칭 리포트 생성 및 통계 제공

### 3.2 백엔드 구조

#### API 엔드포인트
```43:59:backend/app/routers/matching.py
@router.post("/run", response_model=MatchingResponse)
async def run_matching(
    request: Optional[MatchingRunRequest] = None,
    current_user: User = Depends(require_admin),
    session: Session = Depends(get_session),
):
    """N차원 분류 기반 멘토-멘티 매칭 실행"""
    service = MatchingService(session)
    cohort_date = request.cohort_date if request else None
    try:
        result = service.match_all(cohort_date=cohort_date)
        return result
    except LearningHistoryNotInitializedError as exc:
        raise HTTPException(
            status_code=400,
            detail=str(exc),
        ) from exc
```

#### 매칭 알고리즘

**가중치 설정**:
```26:36:backend/app/services/matching_service.py
    # 가중치: 팀 > 약점-강점 > 커리어/거주지/입사년도/연령 > 취미 > 전공 > 성별
    # 밸런스 조정: 분포를 중앙으로 몰기 위해 연속형 요소 비중 확장
    WEIGHT_TEAM = 2.5
    WEIGHT_WEAKNESS_STRENGTH = 2.0
    WEIGHT_CAREER = 1.2
    WEIGHT_CITY = 1.0
    WEIGHT_JOIN_YEAR = 1.0
    WEIGHT_BIRTH_YEAR = 0.8
    WEIGHT_HOBBY = 0.8
    WEIGHT_MAJOR = 0.5
    WEIGHT_GENDER = 0.2

    MAX_MENTEES_PER_MENTOR = 2
```

**매칭 프로세스**:
```43:200:backend/app/services/matching_service.py
    def match_all(self, cohort_date: Optional[date] = None) -> Dict[str, Any]:
        """모든 멘티와 멘토를 매칭"""
        mentee_query = select(TrainingCenterRecord).where(
            TrainingCenterRecord.employee_type == "mentee"
        )
        mentor_query = select(TrainingCenterRecord).where(
            TrainingCenterRecord.employee_type == "mentor"
        )
        if cohort_date:
            mentee_query = mentee_query.where(
                TrainingCenterRecord.cohort_date == cohort_date
            )
            mentor_query = mentor_query.where(
                TrainingCenterRecord.cohort_date == cohort_date
            )

        mentees = self.session.exec(mentee_query).all()
        mentors = self.session.exec(mentor_query).all()

        if not mentees or not mentors:
            return {
                "message": "멘티 또는 멘토가 없습니다.",
                "matched_count": 0,
                "overall_score": 0.0,
            }

        # 학습 이력(초기 성적) 선행 여부 확인
        self._ensure_learning_history_initialized(mentees)
```

**학습 이력 검증**:
```291:341:backend/app/services/matching_service.py
    def _ensure_learning_history_initialized(
        self, mentees: List[TrainingCenterRecord]
    ) -> None:
        """모든 멘티 계정에 초기 학습 이력(초기 점수)이 존재하는지 확인."""
        if not mentees:
            return

        employee_numbers = {
            mentee.employee_number for mentee in mentees if mentee.employee_number
        }
        if not employee_numbers:
            raise LearningHistoryNotInitializedError(
                "멘티 사번 정보가 없어 학습 이력 상태를 확인할 수 없습니다. 연수원 데이터를 다시 동기화한 뒤 시도해주세요."
            )

        mentee_users = self.session.exec(
            select(User).where(
                User.employee_number.in_(employee_numbers),
                User.role == UserRole.MENTEE,
                User.is_active == True,  # noqa: E712
            )
        ).all()

        if not mentee_users:
            raise LearningHistoryNotInitializedError(
                "멘티 사용자 계정이 아직 생성되지 않았습니다. 연수원 연동에서 '계정 생성'과 학습 이력의 '초기 성적 생성'을 완료한 뒤 매칭을 실행해주세요."
            )

        user_ids = [user.id for user in mentee_users]
        scored_user_ids = set(
            self.session.exec(
                select(ExamScore.mentee_id).where(
                    ExamScore.mentee_id.in_(user_ids),
                    ExamScore.exam_type == ExamType.BEGINNING,
                )
            ).scalars().all()
        )

        missing_users = [user for user in mentee_users if user.id not in scored_user_ids]
        if missing_users:
            preview = ", ".join(
                f"{user.name or '미등록'}({user.employee_number or user.email})"
                for user in missing_users[:5]
            )
            additional = (
                f" (예: {preview})" if preview else ""
            )
            raise LearningHistoryNotInitializedError(
                f"학습 이력(초기 평가) 성적이 없는 멘티가 {len(missing_users)}명 있습니다.{additional} "
                "관리자 대시보드 > 학습 이력 탭에서 '성적 생성'을 먼저 실행한 뒤 다시 시도해주세요."
            )
```

### 3.3 프론트엔드 구조

#### UI 컴포넌트
```6000:6054:frontend/src/pages/Dashboard.tsx
    <div className="space-y-6">
      <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
        <div>
          <h2 className="text-xl font-semibold text-gray-900">멘토-멘티 매칭 시스템</h2>
            <p className="text-sm text-gray-600 mt-1">N차원 분류 기반 매칭 (팀 &gt; 약점-강점 &gt; 커리어 &gt; 거주지 &gt; 취미 &gt; 전공)</p>
        </div>
        <button
          onClick={runMatching}
          disabled={matching}
          className="inline-flex items-center justify-center bg-primary-600 text-white px-4 py-2 rounded-lg hover:bg-primary-700 disabled:opacity-50"
        >
          {matching ? '매칭 중...' : '매칭 실행'}
        </button>
      </div>
```

#### 리포트 표시
- 전체 통계 (멘티 수, 멘토 수, 매칭된 쌍, 평균 점수)
- 팀별 통계 테이블
- 매칭 점수 시각화 (바 차트, 히스토그램)
- 가중치 계산식 모달

### 3.4 발견된 이슈 및 개선점

#### ✅ 정상 동작
- 학습 이력 검증으로 사전 조건 확인
- 가중치 기반 매칭 알고리즘
- 멘토당 최대 멘티 수 제한 (2명)
- 매칭 결과를 자동으로 MentorMenteeRelation에 반영

#### ⚠️ 개선 필요 사항
1. **에러 메시지 개선**
   - 학습 이력이 없을 때 명확한 안내
   - 누락된 멘티 목록 상세 표시

2. **매칭 알고리즘 최적화**
   - 현재는 그리디 알고리즘 사용
   - 전역 최적화 알고리즘 고려 (헝가리안 알고리즘 등)

3. **매칭 결과 검증**
   - 매칭 품질 지표 추가
   - 재매칭 기능 제공

4. **성능 최적화**
   - 대량 데이터 처리 시 진행률 표시
   - 비동기 처리 고려

---

## 4. 종합 분석 및 권장사항

### 4.1 기능 간 의존성

```
연수원 연동 → 성적 생성 → 매칭 실행
     ↓            ↓            ↓
  데이터 생성   초기 성적    매칭 결과
```

**의존성 체인**:
1. **연수원 연동**이 먼저 실행되어야 함 (멘티/멘토 데이터 생성)
2. **성적 생성**이 다음 단계 (초기 평가 성적 생성)
3. **매칭 실행**이 마지막 단계 (성적 기반 매칭)

### 4.2 공통 개선 사항

1. **에러 처리 강화**
   - 모든 기능에 상세한 에러 메시지 제공
   - 부분 실패 시 롤백 또는 재시도 로직

2. **진행률 표시**
   - 대량 데이터 처리 시 진행률 표시
   - 비동기 처리로 UI 블로킹 방지

3. **검증 로직 강화**
   - 사전 조건 검증 강화
   - 데이터 무결성 검증

4. **로깅 및 모니터링**
   - 상세한 로그 기록
   - 성능 메트릭 수집

### 4.3 우선순위별 개선 권장사항

#### 🔴 높은 우선순위
1. 매칭 실행 전 사전 조건 검증 강화
2. 에러 메시지 개선 및 사용자 안내 강화
3. 진행률 표시 추가

#### 🟡 중간 우선순위
1. 매칭 알고리즘 최적화
2. 데이터 삭제 시 안전장치 강화
3. 성능 최적화

#### 🟢 낮은 우선순위
1. 매칭 결과 재검증 기능
2. 통계 대시보드 고도화
3. 배치 처리 기능 추가

---

## 5. 결론

세 가지 기능 모두 기본적인 동작은 정상적으로 구현되어 있습니다. 다만, 사용자 경험 개선과 안정성 강화를 위한 몇 가지 개선 사항이 있습니다. 특히 에러 처리, 진행률 표시, 사전 조건 검증 강화가 우선적으로 개선되어야 할 영역입니다.

### 주요 강점
- 체계적인 데이터 생성 로직
- 현실적인 가중치 기반 분포 생성
- 안전한 사전 조건 검증

### 주요 개선점
- 에러 메시지 및 사용자 안내 강화
- 진행률 표시 및 비동기 처리
- 매칭 알고리즘 최적화


